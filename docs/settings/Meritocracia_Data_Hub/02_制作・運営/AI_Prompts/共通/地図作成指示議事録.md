📌 作業議事録（指示用・引き継ぎドキュメント）
件名

メリトクラシア地図制作プロジェクト
初期パイプライン構築完了・次フェーズ移行指示

現在の到達点（前提共有）

本プロジェクトにおいて、以下の技術的基盤が正常動作する状態まで到達した。

Python（venv）環境構築済み

GeoJSON データ読込成功

shapely / numpy / PyYAML 導入済み

CLI 起点での地図生成処理が正常動作

HTML（map.html）としてローカル出力・ブラウザ表示確認済み

日本地図データをベースにした描画に成功

現在の構成では、以下のディレクトリ構造を中核とする：

地図制作プロジェクト/
├── actions/
│   ├── generate_map.sh
│   ├── edit_world.sh
│   └── open_html.sh
├── data/
│   ├── japan.geojson
│   └── meritmap/
│       ├── cli.py
│       └── gen_realmap.py
├── dist/
│   └── map.html
├── world.yaml
└── map_start.sh

次フェーズ作業（別空間で実施すること）
フェーズA：表示制御・視認性調整

地図の表示位置（center）

スケール（zoom / extent）

余白・キャンバスサイズの調整

※ gen_realmap.py 側で完結させること

フェーズB：レイヤー構造の設計

以下の概念レイヤーを分離・拡張可能な構造にする：

海（背景）

陸地ポリゴン

国境線

都市・拠点（Point）

勢力圏（色分け）

world.yaml から制御できる設計を推奨。

フェーズC：架空世界（メリトクラシア）対応

日本地形を「地形テンプレート」として流用

国名・勢力名・境界を world.yaml 側で上書き

現実地名は最終的に完全排除可能な構造にする

フェーズD：将来拡張（現時点では未実装）

クリック／ホバーによる情報表示

ゲーム用マップ・設定資料用マップの分岐出力

SVG / PNG / PDF への派生出力

注意事項

既存の CLI → HTML 生成パイプラインは壊さない

実験的変更は必ずブランチ／別コピーで行う

成功した変更のみ本系統へマージすること