📘 設計書①（全体仕様 / Claude入力用）

※まずこれを丸ごとClaudeに貼る

🧠 プロジェクト概要
目的：
Merit Knowledge Core のDB（KN / SC / HS）を
ブラウザ上で検索・閲覧可能なViewerとして実装する

前提：
・DBはJSON形式で提供される
・ローカル or GitHub Pagesで動作
・依存なし（純HTML / JS）

ゴール：
・検索可能
・一覧表示
・詳細表示
・カテゴリフィルタ
・関係性表示
🏗 システム構成
viewer/
 ├ index.html
 ├ style.css
 ├ app.js
 ├ db.json
🧾 DB仕様（JSON）
[
  {
    "id": "KN-0001",
    "type": "Knowledge",
    "category": "Core/System",
    "summary": "MeritCoreは創作統合OS",
    "detail": "詳細説明...",
    "relation": ["SC-0001", "HS-0001"]
  }
]
⚙ 必須機能
① 一覧表示
id / summary / categoryを表示
② 検索
summary / detail を対象
リアルタイム検索
③ カテゴリフィルタ
Knowledge
Script
History
④ 詳細表示
クリックで展開
detail全文表示
⑤ RELATION表示
関連IDをクリック可能
内部リンクとして機能
📘 設計書②（UI仕様）

※次にこれ貼る

🎨 UI構成
[タイトル]

[検索バー]

[カテゴリフィルタ]
[All][Knowledge][Script][History]

[リスト]
・カード形式
🧩 各要素仕様
■ カード
KN-0001
MeritCoreは創作統合OS
[Core/System]

（クリックで展開）
■ 展開時
DETAIL:
〜全文〜

RELATION:
SC-0001
HS-0001
■ 検索挙動
入力と同時に絞り込み
大文字小文字無視
📘 設計書③（JS仕様）

※これもClaudeに渡す

🧠 ロジック仕様
データ読み込み
fetch('db.json')
状態管理
let data = []
let filtered = []
let currentFilter = "All"
検索処理
function search(keyword) {
  return data.filter(item =>
    item.summary.toLowerCase().includes(keyword) ||
    item.detail.toLowerCase().includes(keyword)
  )
}
フィルタ処理
function filter(type) {
  if (type === "All") return data
  return data.filter(item => item.type === type)
}
レンダリング
function render(list) {
  // DOM生成
}
展開処理
function toggleDetail(id) {
  // 開閉制御
}
RELATIONジャンプ
function jumpTo(id) {
  // 해당カードへスクロール
}
📘 設計書④（Claudeへの指示文）

これが一番重要👇
👉 これも一緒に貼る

あなたはフロントエンドエンジニアです。

上記仕様を満たすWeb Viewerを
純粋なHTML / CSS / JavaScriptのみで実装してください。

条件：
・外部ライブラリ禁止
・コードは1ファイルずつ分けて出力
・index.html / style.css / app.js の順で出力
・そのまま動作する完全コードにする
・コメント付きで分かりやすく

追加要件：
・見た目はシンプルだが見やすく
・カードUI
・クリックで展開
・検索とフィルタは同時に機能する

出力形式：
① index.html
② style.css
③ app.js